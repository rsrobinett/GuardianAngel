var assert = require('assert');         // allows use of assert
var app = require('../app.js').app;     // scope the app to the test suite
var homeFuncs = new HomeTests();        // keeps function scope local


function HomeTests() {

}